# 2026-09-17

## 接入 pi 会话 01a09d91（S-SY-MpApp）
读取并复盘了 pi-hermes-memory 会话 `2026-09-14T01-39-20-794Z_01a09d91-9c59-775f-8774-978604904725`
（154 条消息，2026-09-14 02:04 ~ 2026-09-17 10:07 UTC），了解项目全貌。

核对结果：本地 `HEAD = origin/main = 1d614b1`，工作区干净，与会话结尾一致。

### 项目现状（截至本次）
- 原生微信小程序（JS + WXML + WXSS），无 npm 依赖
- 后端：`https://sy-greece.com`（Website 仓，另一个会话负责，通过 intercom 协作）
- 已联通：`POST /api/leads`、微信登录/绑定手机号、`/api/content`、个人资料 PATCH
- 关键机制：`data/content.js` 的 `loadContent(success, forceRefresh)` + `invalidate()`，
  `app.js onShow` 失效缓存 —— 解决后台改内容不同步的根因
- 契约：`destinations[].attractionId` 显式关联景点；`attractions[].shareTitle/shareImage` 可空回退；
  `mapManagedImage()` 归一化 `./images/` 远程地址并折叠双 `images/` 目录
- 体验版最后上传：1.0.6（对应 07bfc9e）；之后 3 个提交（8e6c721 / 695b9ff / 1d614b1）未上传体验版

### 遗留未决（会话结尾状态）
1. 生产接口未部署 `shareTitle / shareImage / attractionId`（Website c7900b8 未部署）
   → 目的地点击仍提示"暂无详情"
2. 真机手机号绑定需微信客户端短信验证，模拟器不可替代
3. 雅典 6 / 圣托里尼 4 / 纳夫普利翁 2 的 `attractionId` 落点待后台配置

## 18:20 生产接口部署核验（已回 upstairs）
叶荣说生产已部署，实测结论：**代码部署成功，数据未配置**。

### 核验方法（可复用）
1. `curl https://sy-greece.com/api/content?country=greece` 取生产快照（响应 `Cache-Control: no-store`，无 CDN 缓存）
2. 把快照灌进 `data/content.js` 适配器（mock `wx.request` + `getApp`），复现 `index.onDestTap` 判定
   —— 脚本临时放在 `/tmp/verify-live-contract.cjs`，可考虑沉淀进 `scripts/`

### 结论
- ✅ **c7900b8 已生效**：生产 attractions 出现 `shareTitle` key（旧版无此字段），证明是新代码在跑
- ❌ **8/8 目的地 `attractionId` 为空** → 真机点击全部「暂无详情，不跳转」；本地 seed `site-data.json` 同样未配
- ⚠️ **`shareImage` key 在生产响应里消失**：因 `imageUrl(undefined)` 返回 undefined 被 JSON 丢弃。
  属正常现象非 bug —— 小程序 `mapManagedImage(undefined)` 返回 '' 回退主图；后台填值后字段自然出现
- ⚠️ **15/15 景点 `shareTitle` 全空** → 分享文案走「景点名 + 主图」回退

### 待办（生产写操作，待叶荣确认）
给 Website 后台配数据：8 个目的地绑景点 + 景点分享文案。推荐映射：
athens→acropolis / delphi→delphi / meteora→meteora / nafplio→nafplio /
santorini→oia / crete→knossos / mykonos 与 zakynthos 无景点保持空

## 09-18 补记：设计稿与设计文档同步 v2.4
- 更新 `S-SY/SY希腊蔚蓝海岸-小程序设计文档.md` 至 v2.4（品牌希腊旅行管家、三语架构、内容后台化链路、目的地 attractionId 直达、分享后台可配、我的页登录/手机绑定）
- 重绘 01/02/03 设计稿：HTML 高保真（html-v2.4/，样式取自真实 WXSS）→ Chrome headless 2x 截图导出替换 PNG；04/05 保留
- 旧 PNG 备份到 `sy-greece-miniprogram/previous-0911/`（从 git HEAD 恢复）
- 数据来源：生产 /api/content 快照 + utils/i18n.js zh-CN 文案 + pages/*.wxss 实际样式

## 09-18 补记2：首页目的地瓷贴全是同一张图的根因
- 现象：真机首页 culture 瓷贴（雅典/纳芙普利翁/科林斯/德尔斐/迈锡尼）全部显示 dest-athens.jpg
- 根因：体验版 1.0.6 = 07bfc9e，其 adaptDestinations 仍用 mapImage+IMAGE_FALLBACK；
  后台新上传的 destination-mu6x*.jpg 不在映射表 → 全部兜底 dest-athens.jpg。
  mapManagedImage 远程映射是 8e6c721 引入，体验版落后 3 提交未更新
- 验证：生产 /api/content 正常（后台把 5 个 culture 目的地改名换图，id 未变）；
  远程图 curl 全 200
- 附带修复：24a1279 给 dest-img / guide-entry-avatar 加 webp 属性（iOS 远程 webp 默认不解析）
- 待办：上传新体验版（CLI 只能推开发版，设体验版需 mp 后台手动）；
  后台 5 个 culture 目的地 attractionId 仍为空，点击依旧「暂无详情」

## 09-18 补记3：CLI 上传 1.0.7
- `/Applications/wechatwebdevtools.app/Contents/MacOS/cli upload` 成功：版本 1.0.7（含 8e6c721/695b9ff/1d614b1/24a1279），包体 2.0MB
- IDE 需已启动且登录（`cli islogin` 验证）；端口 4200 受 Clash 代理干扰返回 502，无碍
- 设为体验版需叶荣在 mp.weixin.qq.com 后台手动：版本管理 → 开发版本 → 选为体验版
